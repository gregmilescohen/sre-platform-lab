[project]
name = "pulseboard-worker"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "google-cloud-pubsub>=2.21.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=5.0.0",
    "ruff>=0.9.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v"
