name: pulseboard-worker CI

on:
  push:
    branches: [main]
    paths: ["apps/pulseboard-worker/**"]
  pull_request:
    branches: [main]
    paths: ["apps/pulseboard-worker/**"]

jobs:
  lint:
    name: lint
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/pulseboard-worker
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
      - run: uv sync --group dev
      - run: uv run ruff check .
      - run: uv run ruff format --check .

  test:
    name: test
    runs-on: ubuntu-latest
    permissions:
      checks: write
      contents: read
    defaults:
      run:
        working-directory: apps/pulseboard-worker
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
      - run: uv sync --group dev
      - run: uv run pytest tests/ -v --junitxml=junit.xml --cov=app --cov-report=xml
      - uses: dorny/test-reporter@v1
        if: always()
        with:
          name: pulseboard-worker tests
          path: apps/pulseboard-worker/junit.xml
          reporter: java-junit
      - uses: codecov/codecov-action@v4
        if: always()
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: apps/pulseboard-worker/coverage.xml
          flags: pulseboard-worker
          fail_ci_if_error: false
